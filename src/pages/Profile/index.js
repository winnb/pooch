// React
import React from "react";

// Components
import ProfileForm from "../../components/ProfileForm";

const Profile = () => {
  return (
    <div>
          <ProfileForm/>
    </div>
  );
};

export default Profile;
